int(m_vecTile[iIndex]->fX + m_ptScroll.x) < 0 &&	// 메이플 브금 사이트


/// 스테이지별로 TILEX, TILEY 구해주기

/// 인덱스로 타일 X,Y좌표 구하기.
x = INDEX - (y * TILEX);
x = (FCX / 2) + (x * TILEFCX); 

y = INDEX / TILEX;
y = (FCY / 2) + (y * TILEFCY);


/// 인벤토리칸 사이즈.
0번칸 = fX - 60; , fY - 100;

/// 장비창 사이즈.
WEAPON = fX - 40;, fY + 30;
ARMOR = fX, fY + 30;
GLOVE = fX + 40,fY + 65;

60 + (35 * y), -100 + 35 * x;

10;

for (int i = 0; i < 6; ++i)
{
	for (int j = 0; j < 4; ++j)
	{
		index / 4;
		x = 2
		y = 2;
		m_vec[index] == NULL;
	}
}